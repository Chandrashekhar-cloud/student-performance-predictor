# Frontend Environment Variables
# In local development, if this variable is not provided, the frontend automatically falls back to http://127.0.0.1:5000
# In production on Vercel, set this variable to your Render backend URL (without trailing slash):
VITE_API_URL=https://your-render-backend-url.onrender.com
